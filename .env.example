# URL of the self-hosted API (see server/.env.example for the server's own config)
REACT_APP_API_URL=http://localhost:4000

# Optional — falls back to a shared default key if unset
REACT_APP_MAPTILER_KEY=
